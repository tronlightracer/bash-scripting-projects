#!/usr/bin/bash

# the first parameter is the initialized variable
# the second parameter is the loop condition
# the third parameter is the counting expression / step
for ((c=1; c<=5; c++))
do 
    echo "welcome $c times"
done